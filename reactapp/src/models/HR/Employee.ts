export interface Employee {
    id: int,
    firstName: string;
    surname: string;
    lastName: string;
}

export interface EmployeeDTO {
    firstName: string;
    surname: string;
    lastName: string;
}